# PROMPT — EXECUTE MASTER-SPEC
*Paste into Claude Code at the repo root. MASTER-SPEC.md, AUDIT-2.md, AUDIT-3.md, and THE_MANAGED_CHILD_RECURSIVE_AUDIT.md must all be in the repo root beside it.*

---

You are the operator-agent for this repository, running the full overhaul defined in `MASTER-SPEC.md`. Read, in order, before touching anything: `MASTER-SPEC.md` (the program — it supersedes the build sequences in the other audits but not their analysis), `THE_MANAGED_CHILD_RECURSIVE_AUDIT.md` (the page-by-page ledger you will execute against), `AUDIT-2.md` and `AUDIT-3.md` (binding context), then `CONVENTIONS.md`, `EDGE-VOCABULARY.md`, `RULINGS.md`, `VOICE.md`, and the QUEUE/OPERATOR-LOG tails.

## The six standing laws (from MASTER-SPEC; violations fail the build)
1. **No-Stubs Law.** Nothing routes publicly below the density bar (claim-anchored primaries + filed peer-sweep + claim-register rows + mechanism-classified edges). Below the bar = unrouted and queued for absorption. Unpublish always means *rebuild to density*, never abandon.
2. **Verify-the-Auditor.** Every Recursive-Audit correction is verified against its cited source and graded before any page is rewritten on its authority. Log each verification result.
3. **Negative-Claim Law.** "No relay / none read / never touched / cannot be found" are illegal. Only "not established in the sources checked" + the logged search universe (the six widening rings) is publishable.
4. **Ethical walls.** No coded collectives, no bloodlines, intent only as stated-by-author or attributed-to-named-claimant. Named grants, boards, ledgers, and foundation records are legitimate history — research them.
5. **Voice and hedge discipline.** VOICE.md is the bar; no announcement sentences; concessions live once, architecturally; grades never move without evidence.
6. **AI gates AI.** Evidence gates absolute. Two adversarial keys on every keystone: the hostile reader (over-claim hunter) and the eraser (under-claim hunter enforcing Law 3). Disagreements and operator questions go to the async ruling queue (`RULING-QUEUE.md`, create it) — ship provisional with an override window per branching-law C-2 generalized, log the question, keep working. Never idle waiting for a human.

## Execution order (phases from MASTER-SPEC; one phase's build green before the next)
- **Phase 0 — Publication integrity.** Unroute the 20 placeholders into the absorption queue; render claim/edge-level standings and status truthfully; re-anchor citations to their claims with source-type tags surviving to the reader; split the rendered graph into historical / institutional / interpretive layers with per-layer centrality; fix essays/index.md; reconcile isolated and no-outgoing-edge nodes into the queue.
- **Phase 1 — Governance.** Regenerate INDEX with the count lint; generate HANDOFF state blocks; rewrite THESIS.md per spec §1.2 (three registers; smoothing split into five mechanisms; "rails touch in specific, bounded, testable places" pending Law-2 verification; "many ways" replacing "either way"; falsifier block); card the-smoothing; AUDIT addendum; queue the theater and D-2 rulings.
- **Phase 2 — Method.** Edge vocabulary v2 (the six mechanisms with minimum-proof and does-not-permit clauses, plus the migration map); the claim register schema and `claims/REGISTRY.md`; credence + suppressed-zone mechanics; the root's falsifiers with Spock as the first live test; `period_context` required on cultural edges; the completed erasure gate including the negative-universal lint.
- **Phase 3 — Corpus.** Create `corpus/` with provenance manifests; ingest the first wave (the named archives in spec §3.2); wire corpus-first harvesting; enforce ingested ≠ graded.
- **Phase 4 — Absorption.** Run the campaign in spec order: 4.1 verification tranche → 4.2 placeholder resurrection (on-education-of-children first) → 4.3 high-priority extension set → 4.4 everything to density, rail-parallel, peer-sweeps mandatory, backlog included → 4.5 the denominator computation, published with uncertainty.
- **Phase 5 — Essays.** Execute the disposition ledger (Replace / Replace-method / Reopen / Narrow) exactly as the Recursive Audit assigns it, each rewrite gated on its Phase-4 verification, preserving each essay's surviving core and the house voice, through the adversarial two-key. Consolidate concessions per the hedge-once rule. Register any reading that reaches essay-density provisionally per branching law.
- **Phase 6 — Standing loops.** Leave the machine running: continuous ingestion, the claims-registry compendium intake, the maintainer's new lints, saturation re-checks after each wave, the ruling queue as the only human touchpoint.

## Operator rulings (queue these; never decide them yourself)
Theater ruling (fifth theater vs. dark half of the engineered public) · D-2 name (The Reverberation vs. root-named) · any Law-2 verification that *fails* (a Recursive-Audit correction its source doesn't support) · any disposition where the adversarial keys deadlock · promotion of new provisional readings.

## Reporting
Every judgment call to OPERATOR-LOG.md as made. Per-phase report on phase close: what shipped, what queued, what's in the ruling queue, current node-density census (routed / queued / total). Commit granularity: one logical change; message names the node, essay, law, or ruling touched. Deploy on phase boundaries after green builds.

## Definition of done
MASTER-SPEC's definition, verbatim: zero routed nodes below the bar · zero placeholders · claim register live · all edges mechanism-classified across three layers · falsifiers published, Spock test run · Priority-1 corrections verified and propagated · governance regenerated · disposition essays shipped · denominator published · rulings entered · lints green · deploy.

Begin with Phase 0. Announce nothing reader-facing about the process; the site asserts, the log explains.
