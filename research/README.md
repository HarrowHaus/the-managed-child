# research/ — the permanent evidence base

This directory is the project's **source of record for evidence**: research harvests
and operator rulings live here, versioned, next to the nodes they justify. The
writing pipeline (researcher → grader → carder → essayist → critic), the
`maintainer`, and every future session read from **here**, not from chat uploads
and never from memory.

## The law of this directory
- **Every node built from a harvest must cite its primary.** No `documented`
  standing without an attached confirming primary (the moat). This is enforced
  mechanically by the grade-vocab guard and the edge-lint; it is enforced
  editorially here.
- **Addenda win over parent rulings.** Where a `*-ADDENDUM.md` conflicts with its
  parent rulings file, the addendum governs.
- **Search before grading** (per `crowley-rail-RULINGS-ADDENDUM.md`): a standing is
  assigned only after its documented substrate has been searched for and found —
  never from priors, never "apocryphal because it pattern-matches conspiracy."

## Contents
- `crowley-rail-RULINGS.md` — parent rulings for the Crowley-in-America rail.
- `crowley-rail-RULINGS-ADDENDUM.md` — corrections; **wins on conflict** with the parent.
- `research-index.md` — the earlier Crowley-in-America **spine harvest** (Anchors 1–6:
  Crowley → Agape → Parsons → JPL/Aerojet → Hubbard → Babalon → Anger) and the wider
  graph's primary index.
- `future-threads.md`, `verification-queue.md`, `discard-pile.md` — the expansion
  frontier, the primary-lock queue, and the named-exclusions pile.

## PENDING (not yet supplied)
- **MKUltra / West / June-2026 harvest `.md`** — the researched primaries, grade-
  exceptions ($20,800 / $20,505 / $25,900; Subproject 43 dates; 152-files vs
  7-boxes vs 20,000-pages; Latey date), the O'Neill disclaimer (verbatim), the
  `oversight.house.gov` hearing URLs, and the Olmsted cites. **Required before the
  MKUltra/smoothing rail can be built** — its nodes cannot attach primaries
  without it, and both the addendum and this directory's law forbid building from
  memory. Drop the harvest here and the build proceeds.
